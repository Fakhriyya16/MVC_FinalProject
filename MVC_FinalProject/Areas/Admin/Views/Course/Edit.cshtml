@model CourseEditVM
@{
    ViewData["Title"] = "Edit";
}

<section id="course-edit">
    <div class="container mt-5">
        <div>
            <h2>Course Images</h2>
            <div class="d-flex">
                @foreach (var item in Model.Images)
                {
                    <div class="photo-ctgr mx-3" data-id="@item.Id">
                        <img src="~/assets/img/@item.Name" style="width:100px;height:100px;" />
                        <div class="picture d-flex flex-column mt-2" style="width:100px;">
                            <button data-id="@item.Id" class="btn btn-danger btn-sm delete" @(item.IsMain ? "disabled" : "")>Delete</button>
                            <button data-id="@item.Id" class="btn btn-primary btn-sm make-main" @(item.IsMain ? "disabled" : "")>Make Main</button>
                        </div>
                    </div>
                }
            </div>
        </div>
        <form class="forms-sample" method="post" asp-action="Edit" enctype="multipart/form-data">
            <div class="form-group">
                <label class="float-label">Name</label>
                <input asp-for="Name" class="form-control">
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="float-label">Image</label>
                <input asp-for="NewImages" multiple accept="image/*" class="form-control">
                <span asp-validation-for="NewImages" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="float-label">Category</label>
                <select class="form-control" asp-for="CategoryId" asp-items="@ViewBag.Categories"></select>
            </div>
            <div class="form-group">
                <label class="float-label">Price</label>
                <input asp-for="Price" type="number" step="any" class="form-control">
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="float-label">Rating</label>
                <input asp-for="Rating" type="number" class="form-control">
                <span asp-validation-for="Rating" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="float-label">Instructor</label>
                <select class="form-control" asp-for="InstructorId" asp-items="@ViewBag.Instructors"></select>
            </div>
            <div class="form-group">
                <label>Start Date:</label>
                <input type="date" asp-for="StartDate" name="startDate">
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>End Date:</label>
                <input type="date" asp-for="EndDate" name="startDate">
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary mr-2">Submit</button>
            <a class="btn btn-light" asp-action="Index">Cancel</a>
        </form>
    </div>
</section>

@section js {
    <script src="~/Admin/assets/js/edit.js"></script>
}

